import { NgLoopDirective } from './ng-loop.directive';

describe('NgLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new NgLoopDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
